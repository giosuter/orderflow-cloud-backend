# -------------------------------------------------------------------
# Database (Production)
# -------------------------------------------------------------------
spring.datasource.url=jdbc:mysql://orderflow.zitatusi.dbs.hostpoint.internal:3306/zitatusi_orderflow?sslMode=PREFERRED&allowPublicKeyRetrieval=true&characterEncoding=utf8
spring.datasource.username=zitatusi_oflw01
spring.datasource.password=${DB_PASSWORD}   # read from environment variable

spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDBDialect

spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true

logging.level.org.springframework.boot.autoconfigure.flyway=DEBUG
logging.level.org.hibernate.SQL=DEBUG