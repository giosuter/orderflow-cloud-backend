# ==============================================
# PROD PROFILE \u2013 TEMPORARY H2 CONFIGURATION
# ==============================================

# Context Path (WAR is named orderflow-api.war)
### server.servlet.context-path=/orderflow-api

# ---- H2 In-Memory Database (Production TEMP) ----
### spring.datasource.url=jdbc:h2:mem:orderflow_prod
### spring.datasource.driver-class-name=org.h2.Driver
### spring.datasource.username=sa
### spring.datasource.password=

# JPA / Hibernate
### spring.jpa.hibernate.ddl-auto=none
### spring.jpa.show-sql=false

# Flyway migrations
### spring.flyway.enabled=true
### spring.flyway.locations=classpath:db/migration

# H2 Console disabled in prod (default)
# spring.h2.console.enabled=false

# ============================================
# PROD PROFILE \u2013 MariaDB + Flyway
# ============================================

# Context Path (WAR is named orderflow-api.war)
server.servlet.context-path=/orderflow-api

# ---- MariaDB Production Database ----
spring.datasource.url=jdbc:mariadb://orderflow.zitatusi.dbs.hostpoint.internal:3306/zitatusi_orderflow
spring.datasource.username=zitatusi_oflw01
spring.datasource.password=Green.Garden.70
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# JPA / Hibernate: schema managed by Flyway
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false

# Flyway migrations against MariaDB
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

# If the DB is NOT empty and Flyway complains, we can later add:
# spring.flyway.baseline-on-migrate=true